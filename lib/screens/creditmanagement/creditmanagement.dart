import 'package:flutter/material.dart';
class CreditMangement extends StatelessWidget {
  const CreditMangement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "creditmanagement",style: TextStyle(fontWeight: FontWeight.bold),
      ),
    );
  }
}
