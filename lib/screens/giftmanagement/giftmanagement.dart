import 'package:flutter/material.dart';
class GiftmanagementScreen extends StatelessWidget {
  const GiftmanagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Giftmanagement",style: TextStyle(fontWeight: FontWeight.bold),
      ),
    );
  }
}
